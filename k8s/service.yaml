apiVersion: v1
kind: Service
metadata:
  name: millpond-events  # must match statefulset.yaml serviceName
spec:
  clusterIP: None  # headless — required for StatefulSet
  selector:
    app: millpond-events
  ports:
  - name: metrics
    port: 8000
    targetPort: 8000
